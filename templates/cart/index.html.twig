{% extends 'base.html.twig' %}

{% block body %}
<main class="main-content">

    <div class="container">
        <section class="row">
            <div class="col-12">
                <h1>Panier</h1>
                <table class="table">
                    <thead>
                        <tr>
                            <th>Produit</th>
                            <th>Prix</th>
                            <th>Quantité</th>
                            <th>Total</th>
                            <th>Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for element in data %}
                            <tr>
                                <td>{{ element.plat.libelle }}</td>
                                <td>{{ element.plat.prix / 100 }} €</td>
                                <td>{{ element.quantite }}</td>
                                <td>{{ element.quantite * element.plat.prix / 100 }} €</td>
                                <td>
                                    <a href="{{ path('cart_add', {id: element.plat.id}) }}" class="btn btn-success">+</a>
                                    <a href="{{ path('cart_remove', {id: element.plat.id}) }}" class="btn btn-warning">-</a>
                                    <a href="{{ path('cart_delete', {id: element.plat.id}) }}" class="btn btn-danger">Supprimer</a>
                                </td>
                            </tr>
                        {% else %}
                            <tr>
                                <td colspan="5">Votre panier est vide</td>
                            </tr>    
                        {% endfor %}
                    </tbody>
                    <tfoot>
                        <tr>
                            <td colspan="3">Total</td>
                            <td>{{ total / 100 }} €</td>
                            <td>
                                <a href="{{ path('cart_empty') }}" class="btn btn-danger">Vider</a>
                            </td>
                        </tr>
                    </tfoot>
                </table>
                <a href="{{ path('app_cart__form') }}" class="btn btn-primary">Valider le panier</a>
            </div>
        </section>
    </div>
    </main>
{% endblock %}
